# Elova Production Environment Configuration
# Copy this file to .env.production and modify the values for your production deployment
# DO NOT commit .env.production to version control - it contains sensitive information

# Version and Build Info
VERSION=0.1.0
GIT_COMMIT=latest
BUILD_DATE=

# Application Configuration
PORT=3000
NODE_ENV=production

# Database Configuration
DATABASE_TYPE=postgresql
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD

# IMPORTANT: Generate a secure random secret for production!
# You can use: openssl rand -base64 32
AUTH_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_SECRET_MINIMUM_32_CHARS

# n8n Integration Configuration
# These will be set through the setup wizard on first run
N8N_HOST=
N8N_API_KEY=

# Authentication Method
AUTH_TYPE=simple

# Sync Configuration
SYNC_EXECUTIONS=15m
SYNC_WORKFLOWS=6h  
SYNC_BACKUPS=24h

# Optional: External database port (remove to keep internal only)
# POSTGRES_EXTERNAL_PORT=5432

# Optional: Redis configuration
REDIS_PASSWORD=CHANGE_THIS_TO_A_SECURE_REDIS_PASSWORD

# Security Configuration
CORS_ORIGIN=https://your-domain.com
HTTPS_REDIRECT=false

# Setup Wizard (set to false after initial setup is complete)
ENABLE_SETUP_WIZARD=true

# Optional: Supabase Configuration (if using Supabase for auth)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================================================
# SECURITY NOTES:
# 1. Change ALL default passwords before deploying to production
# 2. Use strong, randomly generated secrets
# 3. Keep this file secure and never commit it to version control
# 4. Set appropriate CORS_ORIGIN for your domain
# 5. Enable HTTPS_REDIRECT if running behind HTTPS proxy
# ============================================================================