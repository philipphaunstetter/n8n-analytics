# Elova Docker Compose Configuration
# Copy this file to .env and modify the values as needed

# Application Settings
PORT=3000
DEMO_MODE=true

# Database Configuration (choose one)
# Option 1: Use SQLite (default) - no configuration needed
# Option 2: Use Supabase - uncomment and fill in the values below
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Option 3: Use PostgreSQL - uncomment and use postgres profile
# POSTGRES_PASSWORD=elova_password
# POSTGRES_PORT=5432

# Optional: Redis Configuration
# REDIS_PASSWORD=elova_redis
# REDIS_PORT=6379